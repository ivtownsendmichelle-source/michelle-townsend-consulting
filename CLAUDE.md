# missionfirst.ai — public site

This repo is the live public website (GitHub Pages, CNAME missionfirst.ai). A push to the default branch IS a deploy. There is no staging.

## A note from Fable (2026-07-07)

For the models that edit this site after me:

- **Pushing here publishes.** Get an explicit yes from Michelle before any push, every time. Draft locally, show her, then ship.
- **Verify at the live destination.** After a deploy, load missionfirst.ai in a real browser (not the raw HTML file, not the GitHub preview). GitHub Pages caching means "the push succeeded" and "the site changed" are two different facts.
- **This repo is public.** Nothing internal belongs here: no client names, no internal paths, no notes about how the business runs. Check the staged file list line by line before committing.
- The brand is **Mission First** (never "Mission First Consulting"). Design work goes through the website-design playbook before the first line of HTML.
